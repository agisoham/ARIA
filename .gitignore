# Secrets — NEVER commit these
.env
*.env
!.env.example

# Local publish tooling — stays on your machine, never public
publish.bat
sync.bat
update.bat
scan.py

# Private strategy topics — publish only the neutral template
llm-council/topics/*
!llm-council/topics/_template.md

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS / editor noise
Thumbs.db
.DS_Store
desktop.ini

# Local output (debates are written outside the repo by default; keep any local runs out too)
debates/
